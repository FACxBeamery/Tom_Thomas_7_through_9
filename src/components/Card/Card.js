import React from "react";
import CardImage from "../CardImage/CardImage.js";

const Card = ({ cardData }) => {
	return (
		<CardImage />
		// <CardContent/>
		// <CardBanner/>
	);
};

export default Card;
